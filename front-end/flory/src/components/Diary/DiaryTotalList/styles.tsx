import styled from "styled-components"

export const SMain = styled.main`
  display: flex;
  /* gap: 0.3rem; */
  .date {
    width: 3.3rem;
    font-size: 0.6rem;
    font-weight: bold;
  }
  .diary-content {
    flex: auto;
  }
`
