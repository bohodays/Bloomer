import styled from "styled-components";

export const SMain = styled.main`
  .date {
    font-size: 0.7rem;
    font-weight: bold;
    white-space: nowrap;
  }
  .diary-content {
    flex: auto;
    margin-left: 3.4rem;
    margin-top: -1.1rem;
  }
`;
